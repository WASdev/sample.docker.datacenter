<server>
	<featureManager>
		<feature>clusterMember-1.0</feature>
	</featureManager>

	    <httpEndpoint id="defaultHttpEndpoint"
		  host="${env.HOSTFIX}"
                  httpPort="9080"
                  httpsPort="9443" />
</server>
